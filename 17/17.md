# 17 — Taper #17 submissions ("Prime")

> **Building or editing a piece? Read [AGENTS.md](AGENTS.md) first** — it's the hard
> spec (2 KB limit, template rules, validation, autoplay lesson). This file is the
> creative log / slot tracker.

Up to 5 pieces per author. Deadline **2026-08-17 23:59 AoE**, no extensions.
Submit: zip of HTML files → submit@taperzine.org. Issue launches Fall 2026 at taperzine.org.

Constraint: everything between `</header>` and `</body>` ≤ **2048 bytes**, valid HTML5,
ES6, no external resources, no exec/regex-eval, works offline + mobile.
`<style>` tags inside poem code OK; do not modify template stylesheet.
Official template: `prime-song/template.html` (reference copy) — piece 1 already built on it.
Template body has `overflow:hidden` → pieces must scroll inside their own container.

## Slots

| # | piece | status | source DNA |
|---|---|---|---|
| 1 | `prime-song/` — glossolalic song in a 17-pitch random scale | built (2014 B) | antemelos (phrase/arc/glide/breath) + glossolalia (formant voice, IPA) + aliquoto (prime-partial timbre) |
| 2 | `stealing-prime/` — five prime meridians walked into their colonies | built (2016 B) | Greenwich→Bombay, Paris→Phù Liễn, San Fernando→Manila, Lisbon→Goa, Amsterdam→Batavia; 0° lines random-walk from source to sink under constant drift, stepping at 17\|19\|23\|29\|31 tenths of a second; arrivals counted at each sink |
| 3 | `abakada/` — "aBaKada": Filipino alphabet, primes lit | built (1445 B) | 28 letters, prime positions → B C E G K M O Q U lit in strip; NG at 16=2⁴ dark. Tagalog lexicon (oo mo ko buo buko kubo kuko ube ubo gugo bukbok bugbog kubkob ugok bugok) + English arrivals (be me um emu oboe moo book become queue cue come cube ego q go) — C,Q flash only for English; one unified italic voice (code-switch typography nixed — cohesive language); -um- infix (all prime letters) conjugates both: buko→bumubuko, queue→qumuqueue, emu→umemu. Unpicked English inventory: bee gee ebb egg eke gem keg beg bog cog cob mob mug bug cub gum geek meek boom cook comb combo gumbo gecko cuckoo kook muck mock buck emcee cuke. Unused strata idea: baybayin (17 chars — prime) / abakada 20 → B K E H N O T W |
| 4 | `same-time-tomorrow/` — combinatorial iroha clock | prototype (1786 B) | 47 historical kana × 46 nonzero strides = 2,162 complete affine readings; prime residues address persistent FIFO kana grafts |
| 5 | — | open | |

## Piece 1 notes — prime song

- Voice source: additive partials at prime harmonics 1·2·3·5·7·11 of f0 (aliquoto move).
- Formants: two bandpass filters; F1 = 55·{5,7,11,13}, F2 = 55·{13,17,19,23,29,37,43} Hz.
  Vowel glyph derived from the prime pair via articulatory grid (F1→height, F2→backness).
- Consonants: filtered noise bursts at 470·{2,3,5,7,11,13} Hz, glyphs `hkpʃts`.
- Scale: 17 random pitches, 98–450 Hz (2^2.2 span), sorted; melodic walk over indices
  with per-line arch/trough bias (antemelos contour), phrase-final lengthening,
  breath rests between lines.
- Verse structure: 3|5|7 lines × 3|5|7 syllables.
- One persistent WebAudio graph; song schedules automation, never rebuilds nodes.
- Byte check: `powershell -File check-bytes.ps1 prime-song/index.html`

## Piece 2 notes — Stealing Prime

Titled "Stealing the Prime" until 2026-08-04; folder was `stealing-the-prime/`.

- Five European prime meridians paired with Asian places where longitude or
  standardized time became consequential: Greenwich→Bombay (resistance),
  Paris→Phù Liễn (legislation), San Fernando→Manila (change of date), Lisbon→Goa
  (adoption from a neighbouring colonial system), and Amsterdam→Batavia (a new
  colonial origin). The pairings deliberately superimpose different periods and
  institutions rather than depicting a single imperial network at one moment.
- No map, no graticule, no caption. Ten city dots with names, and five
  full-height `0°` meridian lines. x uses the equirectangular conversion
  `x = (lon+180)/3.6`, with positions rounded to compact map coordinates; y is
  loosely fitted and carries no claim.
- Each zero random-walks from its source to its sink. Per step the zero moves
  `dir*1 + uniform(-2.5, 2.5)` map units — constant drift, no gravity.
- The drift was a progress-proportional ramp (`0.4 + 1.6*progress`, so 4× stronger
  at the sink than at the source) until 2026-08-04. It measurably worked: mean
  advance per step ran 0.48 → 1.98 across the journey, and the zeros took 1161
  steps inside the first fifth of the route against 215 in the last. It was
  simplified out at Xyh's direction — the image did not need to be gravitational,
  only directed — which freed 23 B and removed a latent runaway. Under the ramp,
  a zero that strayed more than 1.25 arc-lengths behind its source would have had
  negative drift and accelerated away permanently, so the ramp needed a clamp at
  0.4 to stay safe; measured strays reached 1.62 arc-lengths, well past that
  threshold. Constant drift cannot reverse and needs no clamp.
- Measured after the change: advance is flat at ~1.0 through every fifth of the
  route, 30% of steps still go backwards, strays behind the source still reach
  1.27 arc-lengths, and first arrivals fall between 38 and 163 s. Still visibly a
  walk, just an evenly paced one.
- Position is carried unwrapped (`X`) and wrapped only for rendering, so a walk
  can cross the Pacific seam without the arrival test breaking. San Fernando's
  direction is set west, which routes it off the left edge and back in at the
  right — the Manila galleon, and the map's cut left visible.
- On arrival the zero **holds on the sink city for one of its own steps**
  (1.7–3.1 s) before returning to the source, so the theft has a visible moment
  rather than only a counter increment.
- The sink label carries the count: `BOMBAY 13`. This is the accumulation and
  the leaderboard both.
- A second line under each sink prints the live time offset from its wandering
  zero: `+4:48`. Longitude is time — 15° is one hour — so the on-screen gap
  between a zero and its colony is already a clock reading; the label only names
  it. Offsets are signed and wrapped to ±12 h, so a zero that wanders far enough
  west flips its colony's sign, which is the date-line condition the Philippines
  actually resolved by deleting 31 December 1844.
- Departure offsets against the historical values: Bombay +4:48 (Bombay Time was
  +4:51), Phù Liễn +6:58 from Paris (the 1906 legal meridian was +6:57:09),
  Goa +5:31 from Lisbon (+5:32), Manila +8:38 from San Fernando (+8:29), and
  Batavia +6:58 from Amsterdam (+6:48). The differences come from the compact,
  rounded x positions.
- Waypoint cities were cut on 2026-08-04 as clutter, after being built twice
  (routed, then inert). Ten city dots remain: five sources, five sinks.
- One static meridian: **Tordesillas**, a fixed full-height *band* centred at
  x=36.63 (48°08′W, the 1494 Spain–Portugal partition) with a name and no clock.
  Drawn as `stroke-width=.9 opacity=.22`, which spans 46°31′W to 49°45′W — the
  real span of the dispute, the treaty-text reading at one edge and the
  Belém–Laguna reading at the other, disagreeing because the length of a league
  and the choice of Cape Verde island were both unsettled. The one fixed thing on
  the map is also the one blurry thing: it does not know where it is. Costs zero
  bytes over the hairline it replaced, since every number is the same width.
  Labelled
  `TORDESILLAS 1494` — the year is load-bearing. Tordesillas is a town in
  Valladolid, and on a map where every other label sits over a dot at its true
  longitude, the bare name reads as a place located mid-Atlantic. The year makes
  it a treaty. Do not drop it. The label sits low, at y=66, for the same reason:
  clear of the European cluster, and below every dotted city, so it annotates the
  line instead of appearing to mark a point on it. It
  never walks, never arrives, and is never counted — the only thing on the map
  that does not move. Drawn in markup rather than JS because nothing about it
  changes. The Spanish zero crosses it about nine times in five minutes on its
  way west, which is the right accident: it is crossing the line that assigned
  it its half of the world. Washington was built first and swapped out on
  2026-08-04 — same byte cost, but Tordesillas is a meridian used to take,
  where Washington's story is about surrendering one.
- Emergent and worth keeping: at the moment of arrival the sink offset reads
  `+0:00`, because the origin is standing on the colony. The theft completes
  exactly when the colony's time equals the metropole's.
- Ship `i` steps when `tick % p_i == 0` on a 100 ms tick, `p ∈ {17,19,23,29,31}`.
  All five step together only every 6,678,671 ticks — 7.73 days.
- Motion between steps is interpolated. Each ship keeps `a` (position before the
  step) and `b` (after); the rendered position is `a + (b-a)·(u % p_i / p_i)`
  where `u = t/100` unfloored, so the tween phase needs no stored state — it
  falls out of the same modulus that schedules the step. Measured per-frame
  movement at 60 fps: mean 0.008–0.022 map units. The only remaining
  discontinuities are the two intended ones, the seam wrap and the snap home
  from sink to source, and the snap-home jumps measure exactly the arc lengths
  (20/29/23/29), which confirms nothing else teleports.
- The prime schedule survives the smoothing untouched: interpolation changes how
  position is drawn between decisions, not when decisions happen. Tick length is
  the one dial — realignment in days is `tick_ms × 6,678,671 / 86,400,000`. At
  100 ms that is 7.73 days; halving the tick doubles the rate at which the walk
  changes heading and halves the figure.
- Measured over a simulated 10 minutes: first arrivals at 25.5, 98.8, 140.3,
  58.0, 83.7 s; counts 13/8/4/8/5. Manila is always slowest (arc 64 vs 20–29).
  Different every run.
- Sizing: `width:min(100vw,(100vh - 52px)*1.42)` with `height:auto` +
  `aspect-ratio:10/7`. The 52px subtracts the fixed template header; a plain
  `130vh` overflowed at 800×450. Verified fitting at 800×450, 1280×720, and
  980-layout mobile.
- With the caption gone there is no fixed-px text left, so everything scales
  with the viewBox and the piece stays legible on a phone despite the Taper
  template having no viewport meta.
- Byte check: `powershell -File check-bytes.ps1 stealing-prime/index.html`

2026-08-04 — Claude Code — review pass, then rebuilt on the walk mechanism.
1529 → 1709 B (339 left).

First pass fixed four defects in the fixed-path version: a `height:84vh` that let
`preserveAspectRatio` letterbox the map (548 px of dead space at phone width, 446 px
before the caption); a `0°` label at `x+0.8` that fell outside the viewBox at the
Pacific seam; a caption cycle of 9 strings at 4 s, the only composite arithmetic in
the piece; and a `stroke-width:.2` inherited onto the 3 px `0°` text.

That version was still a pure function of elapsed time — identical every run, nothing
accumulating, effectively a very long video. The primes set the realignment period but
were not perceptible. Xyh's redesign replaced it: random walk with a strengthening pull
toward the sink, arrivals counted at the destination city. Mechanism is documented
above. Dropped in the same pass, at Xyh's direction: the route paths, the longitude
graticule, and the caption line with its poem text (archive them from git history if
they are wanted back — the seven caption lines died with it).

Undone / open:

- Walk tuning is a knob, not a settled value. Wander is ±2.5 units/step against a
  constant drift of 1 unit/step. Raising the drift would make the voyages read more
  as passage and less as loitering; lowering it does the opposite.
- Heading changes every 1.7–3.1 s now that position is tweened. If the walk reads as
  too stately, drop the tick to 60 ms (heading changes every 1.0–1.9 s, realignment
  4.64 days) and scale the step terms by 0.6 to keep ground speed constant.
- 32 B free.
- **The statement now carries a large part of the work.** Xyh's call, 2026-08-04:
  the head comment is outside the 2 KB span, so it costs nothing, and the piece
  is partly constituted by it. The 2026-08-04 rewrite no longer claims that Africa
  “was not given a clock” or that the Americas were uniformly “running their own
  zero.” It describes the five Asian destinations as a conceptual cluster selected
  for five different outcomes of longitude and standardized time, not as a complete
  geography of colonial time. **Keep the statement in agreement with the mechanism
  when the mechanism changes.** It is the larger half of the piece and the easier
  half to leave stale.
- If more room is ever needed: build the city dots and labels by assigning
  `K.innerHTML` a template string instead of `createElementNS` plus eleven
  `setAttribute` calls, then query the sink texts back out. Estimated 80–120 B.
  Not attempted; would need the whole verification pass rerun.
- Routed midpoints were built and measured before being cut. Two-leg walks through
  the waypoints worked correctly (verified: leg advance, per-leg arcs, seam crossing
  on leg 2) but the full build came to 2350 B. Golf recovered 132 (a `setAttribute`
  alias, deriving `dir` from the route index, moving the ship stroke off the group,
  moving body and `#K` styling into a `<style>` tag, `p=p<0?0:p>1?1:p`, and a
  cheaper rounding in `O`). Dropping the routing to inert dots recovered the rest.
  If the routing is ever wanted back it costs ~120 B and there is nothing left to
  pay with — the offsets (~213 B) would have to go.
- Deriving label positions from dot positions was tried and reverted. A rule of
  `ly = y - 2.4` saved 38 B and produced nine collisions, because Greenwich (y=20),
  Amsterdam (y=20), Paris (y=21) and Lisbon (y=22) sit within two units of each
  other and the authored `ly` values are what fan them apart. Label placement here
  is data, not computable structure. Do not retry this.
- **Untested against a viewer.** Everything above is measured through DOM probes;
  the Browser pane cannot screenshot in this environment (AGENTS.md §5). Nobody has
  actually watched the piece run. Do that before submitting.
- Not W3C-validated yet (pre-submission pass, AGENTS.md §6.6). Rename `index.html` →
  `stealing_prime.html` for the zip; the byline and license URL already say that.

2026-08-04 — Codex — Rewrote the creative statement and README around five
historically distinct fates of zero: resistance in Bombay, legislation at Phù Liễn,
the Philippine change of date, Goa's adoption of Indian Standard Time, and Batavia as
a colonial prime meridian. Replaced Saigon with Phù Liễn in the poem label, corrected
the omitted Philippine date from 30 to 31 December 1844, and removed the unsupported
continent-wide claims about Africa and the Americas. The motion and coordinates were
otherwise unchanged. Verified the poem region with `check-bytes.ps1` at 2016/2048 B
and ran `git diff --check`. Still undone: visual viewing and W3C validation remain part
of the pre-submission pass.

## Piece 4 notes — Same Time Tomorrow

- Opens with the historical 47-kana iroha in legible 7/5/6/5/7/5/7/5 lines.
- Japanese title: `いろは時計` (“Iroha Clock”). An English translation fades to
  zero over the first 47-tick revolution.
- Clock state at tick `t`: offset `t mod 47`; stride `1 + floor(t/47) mod 46`.
- Every state visits all 47 character identities exactly once. All 2,162 affine
  readings occur before the original offset and stride coincide again.
- At returns 1 and 2, the alphabet modernizes persistently: `ゐ→い`, then `ゑ→え`.
- At later return `r`, prime `P_(r-2)` addresses the one-based position
  `P_(r-2) mod 47` (remainder zero means position 47). A random current kana is
  grafted over that sign.
- Each sign retains two grafts in FIFO order, so base plus overlays show at most
  three letter histories. A third graft sheds the oldest overlay.
- At tick 2,162 the original order returns carrying two spelling changes and 44
  grafting events. The clock is periodic; the prime-residue address sequence is
  not, and the grafted kana are contingent.
- Primes are generated incrementally by trial division: each candidate is tested
  for divisors only through its square root, and the search resumes after the
  previously found prime rather than beginning again at 2.
- This gives the clock a latent computational aging. Chronological ticks remain
  regular while linguistic changes accumulate irreversibly, but discovering each
  future mutation generally demands more searching as the primes grow. The cost
  is negligible during an ordinary viewing; in the poem's distant future, time's
  increasing computational burden is genuinely produced by its mathematics
  rather than represented by an artificial slowdown.
- Byte check: `powershell -File check-bytes.ps1 same-time-tomorrow/index.html`
