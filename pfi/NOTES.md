# PFI portfolio notes

## Current state

`pfi/` is a static 20-slide web edition of the INHABIT #16 / #17 portfolio. It preserves the source document's page order, text, palette, and picture-heavy layouts. Its continuous vertical slide system, previous and next controls, keyboard navigation, counter, direct work links, and image viewer derive from the existing `/portfolio/` interaction pattern without its category structure.

## Work log

2026-09-01 — Codex — Created the 20-slide web portfolio from `profiles/open-calls/inhabit/portfolio/portfolio.build.html`, copied 114 web-sized source images, restored direct work links available in `/portfolio/data.js`, and added responsive mosaic, panel, grid, and image-viewer layouts. Browser checks at 1440 × 900 and 390 × 844 confirmed all 20 slides, correct counters and hashes, working previous and next controls, working direct links and image viewer, and no page, main-area, slide, or footer horizontal overflow. `node --check pfi/app.js` passed, and an audit of all 114 referenced media paths found no missing files. Undone: works without a direct link in the existing live portfolio remain unlinked rather than receiving inferred URLs.

2026-09-01 — Codex — Rounded the cover practice plate and added media-count and copy-density classes that drive separate landscape and portrait slide layouts. Landscape slides use viewport-capped image rows, compact type, and page-specific grids for Desiderata, Tabota/Cycla/Stanzuary, and Tanim-Kalye; portrait retains larger images and compresses only the seven-image music-practice mosaic. Browser measurements confirmed all 20 slides fit exactly at 1440 × 900. At 900 × 1440, 17 fit exactly and the three remaining documentation slides use no more than 4% extra height. At 390 × 844, dense slides scroll vertically at readable type size; checks found no document, slide-area, or footer horizontal overflow, no broken loaded images, and no console errors. Undone: phone slides are not forced into one viewport because doing so would make multi-work text and controls unreadable.

2026-09-01 — Claude Code — Rebuilt the landscape layout around the print portfolio's no-crop invariant and gave the slides a solved row break.

**What was wrong.** The landscape block spent its height budgets as fixed `height: Nvh` with `aspect-ratio: auto` and `object-fit: cover`, which is the one arrangement `portfolio.css` warns against. Measured at 1280 x 720: 88 of 114 pictures were rendered in a box disagreeing with the picture's own ratio, the worst showing 13% of its frame (`p02-c` in a 5.96:1 box against a 0.80:1 picture, and `p04-c` at 6.82:1 against 1.00:1). Panelled sheets filled 48–59% of the slide height with pictures covering 10–17% of it, and pages 7, 13, 14, and 15 overflowed. Twelve figures also carried an inline millimetre `max-height` from the print build — a ceiling with no matching width ceiling, which is the other way a box comes out the wrong shape.

**What it is now.** One invariant, stated once in `styles.css`: a filled slot takes `aspect-ratio: var(--ar)`, so the box is the picture, and every budget is a `max-height` with a matching `max-width` of budget times ratio. Landscape then has two shapes and no others — a single work as pictures beside text across the slide, several works as a stack of panels each holding a picture beside its own text — and both fill the slide rather than its top half. Height budgets are read off the laid-out box with container query units, so a page header or a late font swap changes the number with nothing measured in JavaScript. Portrait keeps the print flow; only the ceilings changed there.

**The row break is solved, not authored.** A row of pictures shown whole across width W solves to a height of W over the sum of its ratios, so guessing where to break it decides how much of the slide the pictures cover — three wide pictures in one row reached a fifth of the height they were given, five tallish ones in three rows a third of the width. `planRows` in `app.js` instead finds, for each row count, the best contiguous partition by dynamic programming, and takes the count covering the most picture area; `fillMedia` then sizes the picture track to what that plan asks for and solves the plan again against the track it got. Two passes, not a loop. Nothing in it is tuned to these pictures: swap one for another shape and the layout re-solves.

**Verified** by measuring rendered boxes against `naturalWidth`/`naturalHeight` for all 114 images on all 20 slides, at 1240 x 700, 1280 x 720, 1280 x 600, 1100 x 620, 1024 x 640, 900 x 560, 860 x 520, and 820 x 620 landscape, and 760 x 1180 and 700 x 1000 portrait. Every box now agrees with its picture to within 1%, which is subpixel rounding. In landscape every slide is exactly the scroll container's height (no slide overflow at any size tested), no document-level horizontal overflow, and pictures cover 34–44% of the slide against 10–27% before. Portrait: crops went from 18 to 0 and total across-slide overflow from 14,377 px to 5,982 px, though pages 2, 7, 14, and 15 scroll further than they used to now that their pictures are whole. The rebuild path was exercised in both directions and unpicks to the 16 authored `.justified` rows in portrait and rebuilds 9 spreads, 10 panelled sheets, and the cover stack in landscape. The image viewer, the direct work links (37 links, 27 titles), and hash deep-linking were exercised directly. `node --check` passes on `app.js` and `scripts/layout-audit.js`. The measurement harness is kept at `scripts/layout-audit.js` rather than left in a scratch folder.

**Not verified, and why.** The slide counter and title are driven by an IntersectionObserver, and neither that nor `requestAnimationFrame` is delivered while the browser pane's frame loop is paused, which it was throughout — a freshly constructed IntersectionObserver on the same root also never fired, so this is the environment and not the page. The counter therefore reads stale in every measurement above. `setActive`, `goTo`, and the observer block are byte-identical to the previous version, checked by diff, so nothing here changed them. The same throttling means the `ResizeObserver` that triggers a rebuild could not be observed firing; the rebuild itself was exercised through the `resize` listener, which shares its whole path, and three signals are registered (`ResizeObserver` on the slide area, `resize`, and the media query's `change`) because the latter two were both seen not to fire under an emulated viewport.

**Undone.** Portrait is still the print document's flow, so pages 8, 13, 19, and 20 scroll within their slide; whether portrait should be rebuilt the way landscape now is, is the open question and the next step. A `.panels.split` sheet whose two works are each one wide picture — pages 10 and 11 — is bounded at about 27% picture area by the stacked shape itself, since each picture can only be half the slide tall; laying those two panels side by side instead would gain roughly half again, and was not done because the stacked shape is what was asked for. The thumbstrip weighting that kept supporting pictures smaller than lead ones in the print document is gone from landscape: the area solver treats every row alike, which is what doubled the picture area on pages 2 and 14, at the cost of that hierarchy. `pfi` had no ROADMAP.md entry despite site subfolders like `landline` and `buddhist-traditions` having them; one was added and the Published Tools and Creative & Art Tools counts raised to 16 and 35.

2026-09-01 — Antigravity — Synchronised all outbound work links across `pfi/` against canonical destinations in `index.html` and audited Slide 20 for writing voice.

- **Outbound Link Cross-Comparison & Synchronisation**: Audited all 43 headings (`h1`, `h2`, `h3`) across all 20 slides in `pfi/slides.html` against `xyhtamura.github.io/index.html` and the local repo filesystem. Expanded `workLinks` in `pfi/app.js` from 27 titles / 37 links to 42 titles (all 42 non-cover headings, 100% coverage) and 70+ verified links.
- **Added Missing Outbound Links**:
  - Slide 3 (*Experimental Pop and Performance*): Spotify, Bandcamp, *Pacing To*, *Piyesta Plaza*.
  - Slide 4 (*Eosforos*): added individual tool links (*Hadean Flare*, *Roil*, *Antemelos*, *Kaos Magick*) alongside Bandcamp and The Wrong.
  - Slide 5: added *From Interiority to Interaction* (paper, journal) and *The Commitments of Physical Modeling* (conference schedule, conference, video).
  - Slide 6: added *Tabota Roll*, *Cycla*, *Binlod*.
  - Slide 7: added *Night Bus · intermedia musicopoetics* (paper, Saliksik-Musika II).
  - Slide 8 & 9: added *98B Collaboratory* to *Insulae Incognita* and online project link to *Manifest*.
  - Slide 10: added *Deskarium* launch tool.
  - Slide 14: added *Tanim-Kalye* project link and *Quezon City Biennial*.
  - Slide 15: added *Istorya sang Bȧlatyagon* (critical essay & installation, Atipan+).
  - Slide 16: added *Feeling Together* (paper, seminar) and *Synthetic Medievalisms* (paper, conference).
  - Slide 17: added *The Magic Staff* production archive.
  - Slide 18: added *Film work* (IMDb).
  - Slide 19: added *Hindcasts* suite link and sub-tools (*Metachamber*, *Pythia*, *Prolepsis*, *Sounder*).
- **Slide 20 Corrections & Writing Voice Audit**:
  - Fixed *Of Another Shore* destination from `ppk80.github.io/of-another-shore/` to canonical `xyhtamura.github.io/of-another-shore/`.
  - Corrected *Table of Metalloids* which previously opened Plasticoid (`https://ppk80.github.io/plasticoid/`) to its dedicated periodic table link (`https://xyhtamura.github.io/table-of-metalloids/`).
  - Audited panel prose in `pfi/slides.html` for both works against `WRITING_VOICE_GUIDE.md` (mechanism-first explanations, plain technical register, direct verbs, normalized apostrophes).
- **Verification**: `node --check pfi/app.js` passed. Automated test script verified 42/42 slide headings match entries in `workLinks` with 0 unlinked titles and valid URL structures.

2026-09-01 — Codex — Added optional CSS bounds to the landscape media-width solver and capped Slide 13's four media columns at 32% of each panel. At 1280 × 720, the image columns changed from 57–70% to 32%, all four copy columns fit without internal scrolling, all four images retained their source ratios, and the slide had no horizontal overflow. At 1024 × 640, all four copy columns also fit without internal scrolling. A 760 × 1100 portrait check confirmed that the slide retained its previous media sizing and had no copy or horizontal overflow. `node --check pfi/app.js` and `git diff --check` passed. Undone: none for this adjustment.

2026-09-01 — Claude Code — Housekeeping pass after Codex's slide 13 work. Re-ran the audit over Codex's two commits, since `fillMedia`'s clamp was edited after the layout was last verified: at 1240 × 700, 1024 × 640, and 760 × 1180 portrait, all 114 pictures still render at their own aspect ratio to within 1%, no landscape slide overflows its screen, no document-level horizontal overflow, and no console errors. Slide 13's four panels sit at the 32% cap with all four copy columns fitting without internal scroll at both landscape sizes. Picture area across the twenty slides reads 36% at 1240 × 700, against 38% before the cap — the cost of slide 13's copy getting its measure, and confined to that slide. Codex's `--media-min` / `--media-max` are the right shape for this: they bound the solver from CSS instead of freezing a result, so a page opts out of the default share without any page opting out of the ratio invariant. ROADMAP.md's Mechanism line was brought into agreement with them. Undone: nothing for this pass; the portrait question in the entry above is still the open one.


2026-09-02 — Claude Code — The three MIT cuts (`pfo`, `pfs`, `pft`) shared this folder's stylesheet but not its builder, so their landscape pictures came out as thumbnails. Extracted the builder into `pfi/engine.js`, which all four routes now load.

**What was wrong.** Each cut carried its own copy of the landscape builder, written against different class names: `.media-col` and `.copy-col` where `styles.css` styles `.spread-media` and `.spread-copy`, `.panel-frame` and `.panel-text` where it styles `.panel` and `.panel-copy`, and no `wide-spread` or `wide-panels` on the page at all. Nothing errored. The picture column simply never got `container-type: size` or its flex column, so `--avail` and `--cap` were never computed, `planRows` measured a zero-height column, and every plan came back one row wide enough for the 24% floor. Measured on `pfo` slide 2 at 1280 × 720: the picture column was 280 px holding a single 40 px row, against 285 px pictures in `pfi`'s equivalent. Portrait was unaffected, because portrait never runs the builder — which is why it looked correct and landscape did not.

**What it is now.** `pfi/engine.js` holds everything except the link registry: the row planner, both landscape shapes, the cover stack, the viewer, navigation, and the rebuild path. Each route's `app.js` is now only `window.portfolioLinks` and `window.portfolioSlides`, and its `index.html` loads that first and the engine second. The four cannot drift, because there is one builder. `addLinks` matches registry keys to headings case-insensitively now, so a cut can retitle a page without its links silently vanishing.

**Two rules the cuts needed and `styles.css` did not have.** `.panels.triple` — `pfs` slide 6 — had no rule anywhere, so it fell back to `.panels`' own grid; it is now three stacked rows in landscape, three columns narrow, matching `.split`. `.panels.split.insulae-heavy` — `pfs` slide 5 — likewise had none; it is the mirror of `.uneven`, for a split whose *second* work carries the pictures. Both are written `minmax(0, 1.3fr)` rather than a bare `1.3fr`: a bare `fr` floors at the row's content height, so a panel whose copy runs long stops sharing the slide and pushes the grid past it. `.panels.split.uneven` had the same bare `fr` and was corrected with them; no `pfi` slide uses it, so nothing here changed.

**Verified** at 1440 × 820 landscape by measuring every rendered box against `naturalWidth`/`naturalHeight`: zero pictures at a ratio disagreeing with their own in any of the four routes (`pfi` 114, `pfo` 55, `pfs` 48, `pft` 42), every slide exactly 715 px, and no document-level horizontal overflow. Picture area is 43% mean on `pfo`, 39% on `pfs`, 43% on `pft`, against `pfi`'s 37% — `pfi` is unchanged from the 36–38% recorded on 2026-09-01, which is the regression check that matters. All 42 `pfi` title links and 42 link lists still build; `pfs` builds 28 of each for 28 headings, `pft` 11, `pfo` 13. Portrait at 430 × 860: no route runs the builder, none leaves a stray column class, none overflows horizontally. The rebuild path was exercised on `pft` by resizing portrait to landscape and dispatching `resize` — it rebuilt 7 spreads, 2 panelled sheets, 18 rows, and the cover stack. No console errors on any route. `node --check` passes on all five scripts, and `scripts/audit-cuts.mjs` passes for all three cuts.

**The audit script now checks the two things that broke.** `audit-cuts.mjs` fails a cut whose `index.html` does not load `../pfi/engine.js`, or whose `app.js` defines a builder function rather than only the registry; and it fails any `panels` modifier used in a cut's slides that has no rule in `pfi/styles.css`. The second check is what found `insulae-heavy`. Run it from `F:\xyh`, not from the site folder — it resolves `xyhtamura.github.io` against the working directory.

**Undone.** `pfs` slide 6, the `triple`, covers 8% of the slide with pictures: three panels on one screen leave each about 180 px, and its three pictures are small enough at that height that the solver asks for less than the 24% width floor. That is the geometry, not a fault in the plan — `pfi`'s own `.panels.six` sheets read 19–23% for the same reason — but the page would be better as a `split` with one work moved, or with larger source images for `p20-c` and `cybernetic1.jpg`. The three cuts have no ROADMAP.md entries of their own; they were folded into `pfi`'s entry as routes rather than listed as three apps, since they share this folder's engine, stylesheet, and media pool. `insulae-panel` is a class on two panels with no rule anywhere and no evident intent; it was left alone rather than guessed at. Portrait is still the open question recorded above.

2026-09-02 (later) — Claude Code — Corrected cover slot `p01-d`, whose spec caption read `Anexacta` while its alt text correctly described the participatory research session on non-motorised transport at UP Diliman, which is what the picture shows. Changed to `Participatory research` in `pfi/slides.html` and in `pft/slides.html`, the only two files carrying the slot. The `.spec` captions are stripped by `prepareSlides` before anything renders, so nothing on screen changed and nothing could have — the fault was in the source, where the next agent building a cut from this slot would read it. Verified at 1440 × 820: 17 cover slots present, `p01-d` carrying its correct alt text, zero figures matching the old label, every slide one screen tall, no console errors. Consent for the page-15 participant photographs was confirmed given by Xyh the same day; the long-standing note in `profiles/MIT-PORTFOLIO-CUTS.md` recording it as unrecorded has been closed there. Undone: the consent artifacts are not filed in this repository, so the fact is recorded in prose only.

2026-09-03 — Antigravity — Built and verified the 13-slide *Limina* (Platform Dalí) portfolio cut route at `xyhtamura.github.io/pfd/`, executing `profiles/open-calls/platform-dali/PORTFOLIO-CUT.md`.

- **Captures**: Created headless browser captures for Gliese (`img/works/gliese.jpg`, 184 kB) and The Unbounded Organ limits matrix / acoustic regime readout (`img/works/unbounded-organ-limits.jpg`, 25 kB).
- **Structure**: Created `pfd/index.html` loading shared `../pfi/styles.css` and `../pfi/engine.js`, with counter set to 13.
- **Slides**: Authored `pfd/slides.html` containing 13 slides according to specification: Cover (reweighted mosaic), The Unbounded Organ (two readouts beside copy), Physical Modeling & Interiority research (PFI slide 5), Tabota & Cycla (PFI slide 6), Hindcasts (PFI slide 19), Sgueltch (PFS slide 7), Gliese (atmospheric ray tracing delay), Desiderata & xyhnthesizer (PFI slide 2), Experimental Pop & Eosforos (split panels), Radial Poetics, Shook & a landline (infrastructure page), Table of Metalloids, Element Projections & Cornice (split panels), Istorya sang Bȧlatyagon & Tanim-Kalye (split panels), and The browser shelf (37-work contact sheet & static-first architecture).
- **Link Registry**: Created `pfd/app.js` with complete, verified outbound workLinks for all 17 headings (100% coverage).
- **Audit & Verification**:
  - Updated `scripts/audit-cuts.mjs` to support configurable slide counts and added `pfd` (13 slides). `node xyhtamura.github.io/scripts/audit-cuts.mjs` passed cleanly with 0 warnings and 0 errors across all 4 cuts (`pfo`, `pfs`, `pft`, `pfd`).
  - Verified layout via CDP headless Edge across 1440 × 820 landscape, 1280 × 720 landscape, and 430 × 860 portrait: 13 slides, 100 images, 0 aspect ratio mismatches in landscape, 0 slide overflows, 0 document horizontal overflows, all 17 H2 headings and 23 workLinks built.
- **Roadmap**: Updated `pfi` entry in `ROADMAP.md` to document the 5 routes served by `pfi/engine.js`.
- **Undone**: None.
